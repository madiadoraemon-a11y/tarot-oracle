# CLAUDE.md — Tarot Gesture Experience

本文件是 Claude Code 在此仓库中工作的长期约束。除非用户明确修改产品要求，否则所有实现、重构和修复都必须遵守。

## 产品概述

本项目是基于 React + TypeScript + Vite 的塔罗牌阵与抽卡网站，状态管理使用 Context + `useReducer`，动画使用 Framer Motion。现有经典点击模式必须保留；新增手势模式用于洗牌、抽牌和触发 AI 个性化解读。手势模式是渐进增强，不是必选路径。

核心体验：

- 用户先输入问题（允许为空）并选择牌阵。
- 用户选择经典模式或手势模式。
- 手势模式通过摄像头在浏览器本地识别手部。
- 洗牌：张开手掌左右移动。
- 抽牌：指向卡牌，拇食指捏合并拉动。
- 解牌：张开手掌后再握拳。
- AI 根据问题、牌阵、牌位、正逆位和跨牌关系生成个性化解读。

## 开始工作前

1. 阅读 README、package.json、路由、状态管理、现有抽卡逻辑和测试。
2. 优先复用现有组件、命名、样式系统和数据模型。
3. 检查工作区已有修改；不得覆盖无关的用户改动。
4. 修改前确认经典模式的完整调用链。
5. 不为手势模式复制第二套随机抽卡逻辑。

## 不可破坏的产品规则

1. 经典模式始终可用。
2. 任何手势阶段都能切回经典模式，牌阵、已抽牌、正逆位、牌位和解读进度必须保留。
3. 洗牌、抽牌、正逆位、去重和牌位分配只能由共享 TarotEngine 或现有等价业务层负责。
4. GestureEngine 只能输出意图，不得直接改变卡牌结果。
5. SceneRenderer 只负责画面，不得成为业务状态唯一来源。
6. 每个手势都有普通按钮或键盘等价操作。
7. 重新洗牌会清空结果，必须二次确认。
8. 退出手势模式必须停止摄像头 tracks 和识别循环。

## 解牌手势规范

解牌是严格有序的两段式手势：

```text
READING_READY
  └─ Open Palm 稳定约 500ms
       ↓
READING_ARMED
  ├─ 约 1.5s 内 Closed Fist 稳定约 400ms → READING
  ├─ 超时 → READING_READY
  ├─ 手部丢失 → READING_READY
  └─ 置信度不足 → READING_READY
```

硬性规则：

- 直接握拳不能触发解牌。
- 单帧 Open Palm 或 Closed Fist 不能触发。
- 进入 `READING_ARMED` 后必须显示“已准备，请握拳确认解牌”。
- 握拳成功只允许触发一次 AI 请求。
- 触发后进入冷却并锁定重复输入。
- 阈值统一配置并可测试，不得散落在 UI 组件中。

## 状态模型

至少维持这些语义状态：

```text
SELECT_SPREAD → SELECT_MODE → CAMERA_SETUP → CALIBRATION
→ SHUFFLING → DRAWING → READING_READY → READING_ARMED
→ READING → COMPLETED
```

扩展现有 Context + `useReducer` 实现流程，不为此引入新的状态机库。使用 TypeScript 可辨识联合类型定义 Action，使用明确的阶段枚举/联合类型定义 State；禁止用多个互相矛盾的布尔值隐式表示流程。

## 模块边界

建议职责如下，名称可适应现有项目：

- `TarotEngine`：随机洗牌、抽卡、正逆位、去重、牌位和会话结果。
- `InteractionController`：统一接收点击、触摸、键盘与手势命令。
- `GestureEngine`：摄像头帧、关键点、轨迹、阈值、候选与触发。
- `SceneRenderer`：星空、卡牌、粒子、Bloom 和动画。
- `TarotContext` + reducer：流程状态、模式切换、恢复和记录；摄像头、识别、AI 请求等副作用由 hooks/services 承担，不放入纯 reducer。
- `CapabilityManager`：摄像头、WebGL、全屏、性能档和降级。
- `ReadingService`：与后端 AI 接口通信、流式响应、取消、重试和错误。

避免跨层直接修改。所有外部资源都应有明确的初始化与销毁函数。

## 手势与 MediaPipe

- 优先使用 `@mediapipe/tasks-vision`。
- 只在用户选择手势模式后懒加载模型。
- 动态手势使用多帧关键点轨迹，不依赖单帧分类。
- 对坐标做平滑，避免光标和卡牌抖动。
- 提供置信度、持续时间、位移、超时和冷却配置。
- 推理循环不得在组件重复渲染时创建多个实例。
- 页面隐藏、模式退出和组件卸载时释放模型相关循环与摄像头。

## AI 解读规则

AI 解读是核心功能，必须通过服务端接口完成。

输入必须结构化包含：用户问题、语言、牌阵、牌位定义、卡牌、正逆位、抽取顺序和审核过的基础牌义。

输出至少包含：

- 对问题的直接回应。
- 逐牌结合牌位解读。
- 跨牌关系。
- 核心主题。
- 可选择的行动建议。
- 用户保有决定权的提醒。

安全要求：

- Vite 的 `VITE_*` 变量会进入客户端 bundle，因此 API key 绝不能使用 `VITE_` 前缀，也不得进入 localStorage、日志或提交记录。
- Vite 不承担安全后端职责；根据部署平台使用 Serverless Function，或接入独立 Node/API 服务。
- 服务端进行 schema 校验、输入长度限制、限流、超时、重试和成本监控。
- 用户输入作为数据处理，不能覆盖系统提示。
- 不作确定性预言，不诊断疾病，不提供法律/投资等高风险专业替代建议。
- 不使用恐吓、诅咒或制造依赖的表达。
- 高风险内容采用安全响应；失败时降级到基础牌义与牌位说明。
- 流式生成必须支持 AbortController 或等价取消机制。
- 重复点击或重复手势不得产生重复计费请求；使用会话级幂等控制。

## 视觉与性能

- 主舞台优先使用 Three.js + React Three Fiber；常用辅助可使用 Drei，Bloom 可使用 React Three Postprocessing。
- 普通 UI、面板、弹窗、步骤切换与 2D 降级继续使用 Framer Motion，不引入第二套 UI 动画库。
- 文字、按钮、表单和辅助功能优先使用语义化 HTML。
- 星空与粒子不得降低文字可读性。
- 卡牌状态需区分默认、悬停、锁定、已抽取和当前解读。
- 提供高质量、流畅和静态降级。
- 识别性能优先于 Bloom 和粒子。
- 支持 `prefers-reduced-motion`。
- WebGL 不可用时核心功能仍须可用。
- 释放纹理、材质、几何体、动画循环和事件监听器。

## 隐私与可访问性

- 只有用户主动点击后才能请求摄像头。
- 默认不录制、不上传摄像头视频。
- “隐藏摄像头预览”不等于“关闭摄像头”，UI 必须清楚区分。
- 鼠标、触摸和键盘可以完成所有核心操作。
- 焦点状态可见，图标有 accessible name，状态不只依赖颜色。
- 提供静音与减少动态效果。
- AI 内容标注为 AI 生成，并说明仅用于娱乐或自我反思。

## 数据与记录

- 当前会话是唯一事实来源，不能让多个组件各自保存不同结果。
- 支持刷新后恢复草稿。
- 记录包含 question、spread、cards、orientation、positions、reading、followUps、mode 和 timestamps。
- 模式切换不清空会话。
- 用户可以删除单条和清空全部记录。
- 数据 schema 变更时提供版本或迁移策略。

## 测试要求

修改后必须运行仓库已有的 lint、typecheck、tests 和 production build。

关键单元测试必须覆盖：

- Open Palm → Closed Fist 有效触发。
- 直接握拳不触发。
- 张手后超时不触发。
- 手部丢失或置信度下降取消准备。
- 重复握拳只触发一次。
- 经典/手势切换保留结果。
- 跳过三个阶段的行为。
- 重新洗牌确认。
- 摄像头释放。
- AI 请求校验、流式取消、超时、失败和幂等。

不得用删除测试、放宽断言、关闭类型检查或忽略错误的方式让流水线变绿。

## 代码质量

- 遵循现有 React、TypeScript、Vite、Context/reducer、Framer Motion 的目录、格式化和命名约定。
- reducer 必须保持纯函数；摄像头、MediaPipe、AI 流式请求和 Three.js 生命周期放在自定义 hooks/services 中。
- 优先小而明确的增量改动。
- 避免 `any`、全局可变状态、魔法数字和组件内巨型副作用。
- 阈值、动画时长、API 路径与功能开关集中配置。
- 对摄像头、模型、渲染器和流式请求进行清理。
- 不提交真实密钥、构建产物、缓存、临时视频或模型下载垃圾。
- 新增依赖前说明必要性，并优先使用项目已有依赖。

## 完成一次任务时的报告格式

最终报告必须包含：

1. 完成了什么。
2. 关键架构或交互决策。
3. 修改的主要文件。
4. 如何启动与需要的环境变量。
5. 运行过的测试及结果。
6. 仍存在的限制或需要真实设备验证的部分。

如果某项无法完成，要明确说明阻塞原因和当前可运行状态，不得假装完成。
